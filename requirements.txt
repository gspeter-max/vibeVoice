# Parakeet Flow v2 — pip-compatible dependency list
# Kept alongside pyproject.toml as a fallback for systems where uv has issues.
#
# Install with pip (recommended for this machine):
#   .venv/bin/pip install -r requirements.txt
#
# Install with uv (if uv cache permission is fixed):
#   uv pip install -e . --python .venv/bin/python

# Core STT (replaces torch + nemo_toolkit)
faster-whisper>=1.1.0

# Audio input
pyaudio>=0.2.14

# Keyboard auto-type
pynput>=1.7.7

# Numerics
numpy>=1.26.0